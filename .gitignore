.build/
.swiftpm/
build/
*.xcodeproj
.DS_Store

# The icon artwork itself is not tracked; make-app.sh builds the .icns from
# whatever icon.png is sitting in the root, and the app builds fine without one.
# The glob covers the working copies that pile up beside it while an icon is
# being tried out.
icon*.png
